ShoppingBag is a Java program that reads item prices from a file and selects a subset whose total is as close as possible to a user-defined budget, without exceeding it. It avoids brute-force solutions, handles edge cases, and reports runtime.






